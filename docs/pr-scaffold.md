# Draft PR Scaffold

This placeholder file exists to create an initial commit on `feature/booking-guesty-stripe`.
It enables opening a draft pull request before implementation begins.

Do not merge to `main` until booking/payment work is completed and approved.
